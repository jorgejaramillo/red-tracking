# Copy to .env for local overrides (never commit .env)
# Use the synthetic landmark source instead of the webcam (tests/demos)
WXT_FAKE_CAMERA=0
# Grant localhost host permissions at install (Playwright e2e builds)
WXT_E2E=0
